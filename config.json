{
  "model_type": "hierarchical_transformer",
  "hidden_size": 1600,  // Ensure this aligns with your model's architecture
  "num_attention_heads": 16,  // Must be a divisor of hidden_size
  "num_hidden_layers": 36,  // Ensure this is appropriate for your model
  "intermediate_size": 6400,  // Typically 4x hidden_size
  "hidden_act": "gelu",  // Activation function
  "initializer_range": 0.02,  // Initializer range for weights
  "layer_norm_eps": 1e-12,  // Epsilon for layer normalization
  "vocab_size": 50000  // Size of the vocabulary
}
}
